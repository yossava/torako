<%= form_for(@subcategory) do |f| %>
  <% if @subcategory.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subcategory.errors.count, "error") %> prohibited this subcategory from being saved:</h2>

      <ul>
      <% @subcategory.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :subkategori %><br>
    <%= f.text_field :subkategori %>
  </div>
  <div class="field">
    <%= f.label :deskripsi %><br>
    <%= f.text_area :deskripsi %>
  </div>
  <div class="field">
    <%= f.label :subcategori_image %><br>
    <%= f.text_field :subcategori_image %>
  </div>
  <div class="field">
  <%= f.collection_select :category_id, Category.order(:kategori), :id, :kategori, {:prompt => "Pilih Kategori"} %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
